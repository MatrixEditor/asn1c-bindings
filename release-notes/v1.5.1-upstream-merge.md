# v1.5.1 Upstream Merge Notes

  git fetch upstream
  git switch <feature-branch>

  # bring in upstream
  git merge upstream/vlm_master

  # apply the integration/conflict fixes I committed here
  git cherry-pick 51d8209d

  # verify
  git status
  git log --oneline -3

  If creating a PR branch from the completed work:

  git switch vlm_master
  git switch -c merge-upstream-mouse07410
  git push -u origin merge-upstream-mouse07410
## Upstream
- Merged `mouse07410/asn1c` `vlm_master` through `388bbe28`.
- Preserved this fork's Python binding generator defaults, generated module/stub output, Python-specific datadir options, and reserved keyword handling.

## Integration Fixes
- Reconciled upstream compiler, fixer, printer, and skeleton changes with the fork's Python generation hooks.
- Restored malformed auto-merged skeleton codec files to the upstream implementations where the fork had no Python-specific behavior.
- Fixed example makefile cleanup so generated PDU collection files do not leave stale build state after `distclean`.
- Adjusted XER SET OF / SEQUENCE OF handling for synthetic collection member names so generated PKIX XER data accepts actual element tags.
- Updated C-only regression harnesses to explicitly disable Python generation where the tests are validating generated C output.

## Validation
- `make -j1` passes.
- Example regression builds pass for `sample.source.PKIX1`, `sample.source.LDAP3`, `sample.source.MEGACO`, and `sample.source.RRC`.
- `tests/tests-asn1c-smoke` passes 24/24 after explicit C-only test flags.
- `tests/tests-asn1c-compiler` passes 3/3 after explicit C-only golden output flags.
- `tests/tests-c-compiler` improved from 47 failures to 23 failures after the C-only harness update. Remaining failures are not treated as merge blockers for this integration pass:
  - most sampled failures are stale generated Python support files in reused C-compiler test directories;
  - the `119-per-strings` UPER cases reach runtime and trip ASAN, which needs a separate PER-focused investigation.

## Notes
- `../icspacket` did not contain ASN.1 files under the searched tree, so regression coverage used the repository examples and tests.
- Python development headers were required for generated Python binding builds.
